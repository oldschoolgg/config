/**
 * Default ESLint configuration for oldschoolgg Projects
 * @example
 * ```json
 * {
 *   "extends": "@oldschoolgg/config"
 * }
 * ```
 */
export const config = {
	extends: ['@sapphire']
};

module.exports = config;
export default config;
